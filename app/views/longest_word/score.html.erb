<div class="container">
  <h1 class="text-center">Your score</h1>
  <div class="row">
    <div class="col-xs-12 col-md-6">
        <h2>Your word: <%= "#{@attempt}" %></h2>
        <h2>Time take to answer: <%= "#{@result[:time].round} sec" %></h2>
      </div>
    <div class="col-xs-12 col-md-6">
      <h2>Your score: <%= "#{@result[:score].round}" %></h2>
      <h2>Message: <%= "#{@result[:message]}" %></h2>
    </div>
  </div>
</div>
